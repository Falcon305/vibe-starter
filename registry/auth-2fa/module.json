{
  "name": "auth-2fa",
  "title": "Two-factor authentication",
  "description": "TOTP two-factor authentication with backup codes via Better Auth",
  "category": "auth",
  "version": "0.1.0",
  "provides": ["two-factor"],
  "dependsOn": ["auth-better-auth"],
  "postInstall": [
    "Run pnpm db:generate && pnpm db:migrate to create the two_factor table",
    "Enable with authClient.twoFactor.enable({ password }); verify with authClient.twoFactor.verifyTotp({ code })"
  ],
  "legal": {
    "subprocessors": [],
    "cookies": [],
    "dataCollected": ["TOTP secret and backup codes"]
  }
}
